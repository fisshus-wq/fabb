services:
  backend:
    build: ./backend
    volumes:
      - db_data:/app/data
    environment:
      - DATABASE_URL=sqlite:////app/data/fabb.db
    restart: unless-stopped

  frontend:
    build: ./frontend
    ports:
      - "3000:80"
    depends_on:
      - backend
    restart: unless-stopped

volumes:
  db_data:
